feat: Implement random problem management API endpoints

- Refactor random problem API endpoints to use service and CRUD layers.
- Implement GET /random-problem/{user_id} to fetch problems from DB.
- Implement POST /random-problem/create to generate random coordinates within a circle and save to DB.
- Implement PATCH /random-problem/complete/{random_problem_id} to update problem status to 'completed'.
- Implement PATCH /random-problem/given-up/{random_problem_id} to update problem status to 'given_up'.
- Add `read` method to `RandomProblemCRUD` to fulfill `BaseCRUD` abstract method.
- Update Pydantic models to support ORM mode (`from_attributes=True`).
- Fix various Ruff linting issues (unused imports, formatting, description syntax).